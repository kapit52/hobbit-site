# ── Секреты и локальная конфигурация ──────────────────────────────
.env
password.txt

# ── Рантайм-данные MySQL (bind-mount из docker-compose) ───────────
# Большие файлы (~190 МБ) + приватные ключи (ca-key.pem, client-key.pem)
# + бинлоги. В репозитории не нужны: схема лежит в docker/init/01_database.sql.
docker/mysql-data/

# ── Логи приложения ───────────────────────────────────────────────
# Структуру папки logs/ (.htaccess, index.html) оставляем, сами логи — нет.
logs/*.log

# ── Системное / редакторы ─────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/
.cursor/
*.swp

# ── PHP / зависимости ─────────────────────────────────────────────
vendor/
composer.lock

# ── Тесты (PHPUnit) ───────────────────────────────────────────────
# Сам бинарник phpunit.phar (~5 МБ) не храним в репозитории — он скачивается
# одной командой (см. tests/README.md). Кэш PHPUnit тоже локальный.
tests/tools/phpunit.phar
.phpunit.cache/
.phpunit.result.cache
db.png
order_lifecycle.png
ДИАГРАММЫ.md
guest_reservation_lifecycle.png
варианты использования.png
IDEF0_1.png
